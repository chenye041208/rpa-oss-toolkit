"""
阿里云 OSS 库 - 运行时配置

本文件包含所有可调整的运行时参数，方便其他用户直接修改。
OSS连接参数（AK/SK/endpoint/bucket）请在创建会话时通过参数传入。
"""

# ============================================================================
# 日志配置
# ============================================================================

# 日志文件存储路径前缀（OSS上的目录）
# 会在该路径下创建日志文件，格式：{prefix}{date}_{session_name}.log
LOG_PREFIX = "logs/"

# 日志缓冲区大小
# 达到该数量后自动刷新到 OSS，减少网络 IO
LOG_BUFFER_SIZE = 50

# ============================================================================
# 文件传输配置
# ============================================================================

# 分片上传/下载阈值
# 文件大小超过该值时自动使用分片传输（单位：字节）
# 默认 5MB = 5 * 1024 * 1024
MIN_MULTITHREAD_SIZE = 5 * 1024 * 1024

# 分片大小（字节）
# 默认 10MB = 10 * 1024 * 1024
PART_SIZE = 10 * 1024 * 1024

# 最大重试次数
MAX_RETRY_COUNT = 3

# 重试延迟（秒）
RETRY_DELAY = 2

# ============================================================================
# 会话配置
# ============================================================================

# 会话默认有效期（秒）
# None 表示不过期
# 示例：3600 = 1小时，86400 = 1天
DEFAULT_EXPIRES_IN = 3600

# ============================================================================
# 元数据配置
# ============================================================================

# 下载文件时默认的缓存时间（秒）
DEFAULT_CACHE_MAX_AGE = 3600

def main(args):
    pass
